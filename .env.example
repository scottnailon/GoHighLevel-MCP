# GoHighLevel MCP Server v2 — Configuration

# ---------------------------------------------------------------------------
# AUTHENTICATION (required)
# ---------------------------------------------------------------------------

# Your GoHighLevel Private Integration Token (PIT).
# Generate at: GHL → Settings → Private Integrations → Create new Integration
# Required scopes for full functionality:
#   contacts.readonly, contacts.write
#   conversations.readonly, conversations.write
#   conversations/message.readonly, conversations/message.write
#   calendars.readonly, calendars.write, calendars/events.readonly, calendars/events.write
#   opportunities.readonly, opportunities.write
#   custom-fields.readonly, custom-fields.write
#   workflows.readonly
#   forms.readonly
#   users.readonly, users.write
#
# --- Single client (legacy, still supported) ---
GHL_API_KEY=

# Your Sub-Account / Location ID. Get from GHL → Settings → Business Profile.
GHL_LOCATION_ID=

# --- Multiple clients (recommended if you manage more than one GHL account) ---
# Set GHL_CLIENTS instead of GHL_API_KEY/GHL_LOCATION_ID to configure several
# clients on one server. Each key is the client's Location ID; add a new entry
# to add a new client — no other config changes needed.
#
# GHL_CLIENTS={"<location_id_1>":{"api_key":"pit-xxxx","label":"Client A"},"<location_id_2>":{"api_key":"pit-yyyy","label":"Client B"}}
#
# Every tool accepts an optional `location_id` param to pick which client's
# credentials to use for that call. If omitted, resolution falls back to:
#   1. GHL_DEFAULT_LOCATION_ID below (if set)
#   2. the sole configured client (if there's only one)
#   3. otherwise the call fails with an error listing the configured clients
#
# GHL_DEFAULT_LOCATION_ID=<location_id_1>

# ---------------------------------------------------------------------------
# API CONFIGURATION (optional, sensible defaults)
# ---------------------------------------------------------------------------

# API base URL. Almost never needs changing.
GHL_BASE_URL=https://services.leadconnectorhq.com

# API version header. Pin to a known-good version.
GHL_API_VERSION=2021-07-28

# HTTP request timeout in seconds.
GHL_TIMEOUT=30

# Maximum retries on rate-limit (429) and transient errors.
GHL_MAX_RETRIES=3

# ---------------------------------------------------------------------------
# LOGGING (optional)
# ---------------------------------------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR. Defaults to INFO.
# Note: stdio MCP servers log to stderr, not stdout.
GHL_LOG_LEVEL=INFO
